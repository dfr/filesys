{
  "name": "nfsd-ui",
  "private": true,
  "version": "0.0.1",
  "description": "Management UI for nfsd",
  "license": "BSD",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "browser-sync start --proxy localhost:20249 --serveStatic ui --files ui"
  }
}
